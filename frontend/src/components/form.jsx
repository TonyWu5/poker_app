import React from 'react';
import Sessions from './sessions';

const Form = (props) => {
    return (
      <div>
        <h1>View Sessions</h1>
        <Sessions />
      </div>
    );
};

export default Form;